name: fuzz
on:
  workflow_dispatch:
  workflow_call:
jobs:
  fuzz:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'schedule'
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: fuzz-rax
            pwd: ./fuzz/fuzz-rax
          - target: fuzz-rax-nmea
            pwd: ./fuzz/fuzz-rax-nmea

    runs-on: ubuntu-latest
    steps:
      - parallel:
          - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
          - uses: Glatzel/template/.github/actions/rust/setup-rust@f8988acee66029caca381ab62241b0e3e41bd56b # v2026.9.19
            with:
              toolchain: nightly
              components: llvm-tools-preview
      - uses: Glatzel/template/.github/actions/rust/cargo-fuzz@f8988acee66029caca381ab62241b0e3e41bd56b # v2026.9.19
        with:
          working-directory: ${{matrix.pwd}}
          target: ${{matrix.target}}
      - uses: Glatzel/template/.github/actions/common/codecov@f8988acee66029caca381ab62241b0e3e41bd56b # v2026.9.19
        if: ${{!cancelled()}}
        with:
          flags: ${{matrix.target}}
